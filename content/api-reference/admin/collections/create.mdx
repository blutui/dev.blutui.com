---
title: 'Create collection'
description: 'Create a collection in a project.'
api: 'POST {handle}.blutui.com/admin/api/collections'
---

### Body parameters

<Parameter name="name" type="string" required>
The name of the collection.
</Parameter>

<Parameter name="handle" type="string" required>
The unique handle for this collection.
</Parameter>

### Response

```ts tab="Node.js"
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.project('one').admin.collections.create({
  name: 'Products',
  handle: 'products',
})
```

```bash tab="cURL"
curl -X POST 'https://{handle}.blutui.com/admin/api/collections' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
     -d $'{
  "name": "Products",
  "handle": "products"
}'
```

<include>./collection-response.mdx</include>

<include>./collection-response-parameters.mdx</include>
